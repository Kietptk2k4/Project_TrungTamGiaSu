// If the file is actually in a different location, update the path
import "../../styles/components/tutor-list.css"; // If the file has a different name
// OR
import "../../../styles/components/tutors-list.css"; // If the file is at a different level
